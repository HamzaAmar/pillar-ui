import { SvgType } from '../../type'
import BigArrowDown from './bigArrowDown'

const BigArrowLeft = ({ direction, ...rest }: SvgType) => <BigArrowDown direction="left" {...rest} />

export default BigArrowLeft
