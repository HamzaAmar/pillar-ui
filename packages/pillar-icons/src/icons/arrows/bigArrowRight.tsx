import { SvgType } from '../../type'
import BigArrowDown from './bigArrowDown'

const BigArrowRight = ({ direction, ...rest }: SvgType) => <BigArrowDown direction="right" {...rest} />

export default BigArrowRight
