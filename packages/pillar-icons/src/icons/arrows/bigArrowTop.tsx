import { SvgType } from '../../type'
import BigArrowDown from './bigArrowDown'

const BigArrowTop = ({ direction, ...rest }: SvgType) => <BigArrowDown direction="top" {...rest} />

export default BigArrowTop
