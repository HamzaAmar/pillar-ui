// import { describe, test, expect } from 'vitest'
// import { getWeekdays } from '../date'

import { describe, test } from 'vitest'
// import { getEndOfWeek } from '../date' // Replace 'your-module' with the correct path to the module containing the functions.

describe('getStartOfWeek', () => {
  test('should return the start of the week for a valid date string (en-US)', () => {})
})

// describe('getWeekdays', () => {
//   test('should return the long format weekdays in en-US locale by default', () => {
//     const weekdays = getWeekdays()
//     expect(weekdays).toEqual(['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'])
//   })

//   test('should return the long format weekdays for en-US locale', () => {
//     const weekdays = getWeekdays('en-US', 'long')
//     expect(weekdays).toEqual(['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'])
//   })

//   test('should return the short format weekdays for en-US locale', () => {
//     const weekdays = getWeekdays('en-US', 'short')
//     expect(weekdays).toEqual(['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'])
//   })

//   test('should return the narrow format weekdays for en-US locale', () => {
//     const weekdays = getWeekdays('en-US', 'narrow')
//     expect(weekdays).toEqual(['S', 'M', 'T', 'W', 'T', 'F', 'S'])
//   })

//   test('should return the long format weekdays for fr-FR locale', () => {
//     const weekdays = getWeekdays('fr-FR', 'long')
//     expect(weekdays).toEqual(['samedi', 'dimanche', 'lundi', 'mardi', 'mercredi', 'jeudi', 'vendredi'])
//   })

//   test('should return the short format weekdays for fr-FR locale', () => {
//     const weekdays = getWeekdays('fr-FR', 'short')
//     expect(weekdays).toEqual(['sam.', 'dim.', 'lun.', 'mar.', 'mer.', 'jeu.', 'ven.'])
//   })

//   test('should return the narrow format weekdays for fr-FR locale', () => {
//     const weekdays = getWeekdays('fr-FR', 'narrow')
//     expect(weekdays).toEqual(['S', 'D', 'L', 'M', 'M', 'J', 'V'])
//   })

//   test('should return the long format weekdays for de-DE locale', () => {
//     const weekdays = getWeekdays('de-DE', 'long')
//     expect(weekdays).toEqual(['Samstag', 'Sonntag', 'Montag', 'Dienstag', 'Mittwoch', 'Donnerstag', 'Freitag'])
//   })

//   test('should return the short format weekdays for de-DE locale', () => {
//     const weekdays = getWeekdays('de-DE', 'short')
//     expect(weekdays).toEqual(['Sa.', 'So.', 'Mo.', 'Di.', 'Mi.', 'Do.', 'Fr.'])
//   })

//   test('should return the narrow format weekdays for de-DE locale', () => {
//     const weekdays = getWeekdays('de-DE', 'narrow')
//     expect(weekdays).toEqual(['S', 'S', 'M', 'D', 'M', 'D', 'F'])
//   })

//   test('should return the long format weekdays for it-IT locale', () => {
//     const weekdays = getWeekdays('it-IT', 'long')
//     expect(weekdays).toEqual(['sabato', 'domenica', 'lunedì', 'martedì', 'mercoledì', 'giovedì', 'venerdì'])
//   })

//   test('should return the short format weekdays for it-IT locale', () => {
//     const weekdays = getWeekdays('it-IT', 'short')
//     expect(weekdays).toEqual(['sab', 'dom', 'lun', 'mar', 'mer', 'gio', 'ven'])
//   })

//   test('should return the narrow format weekdays for it-IT locale', () => {
//     const weekdays = getWeekdays('it-IT', 'narrow')
//     expect(weekdays).toEqual(['S', 'D', 'L', 'M', 'M', 'G', 'V'])
//   })

//   test('should return the long format weekdays for es-ES locale', () => {
//     const weekdays = getWeekdays('es-ES', 'long')
//     expect(weekdays).toEqual(['sábado', 'domingo', 'lunes', 'martes', 'miércoles', 'jueves', 'viernes'])
//   })

//   test('should return the short format weekdays for es-ES locale', () => {
//     const weekdays = getWeekdays('es-ES', 'short')
//     expect(weekdays).toEqual(['sáb', 'dom', 'lun', 'mar', 'mié', 'jue', 'vie'])
//   })

//   test('should return the narrow format weekdays for es-ES locale', () => {
//     const weekdays = getWeekdays('es-ES', 'narrow')
//     expect(weekdays).toEqual(['S', 'D', 'L', 'M', 'X', 'J', 'V'])
//   })
// })
